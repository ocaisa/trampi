#!/bin/sh

MPIEXEC="${TRAMPI_ABI_LIBRARY_MPIRUN:-@DEFAULT_MPIRUN@}"

if [ -z "$MPIEXEC" ]; then
    echo "TRAMPI:: No MPI launcher configured." >&2
    echo "TRAMPI:: Please set TRAMPI_ABI_LIBRARY_MPIRUN or configure a default." >&2
    exit 1
fi

exec "$MPIEXEC" "$@"
